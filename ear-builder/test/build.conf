#-- application name and title
app_name=test
app_title=Test App
app_host=localhost:8080

#-- war settings
web_context=test-web

#-- db configuration
db_host=localhost
db_user=root
db_pass=

#-- mysql command (could be the full path of the mysql executable file)
mysql_cmd=mysql
