<?xml version="1.0" encoding="UTF-8"?>
<workunit>
    <invokers>
        <invoker folderid="/menu/file" caption="Controls Test"/>
        
        <!-- form actions -->
        <invoker type="actions" action="_close" caption="Close" immediate="true"/>
        <invoker type="actions" action="edit" caption="Edit"/>
        <invoker type="actions" action="print" caption="Print"/>
        <invoker type="actions" action="btnToggle" caption="Enable/Disable Controls" immediate="true"/>
    </invokers>
    
    <code>
        <![CDATA[
        
        import com.rameses.rcp.annotations.*;
        import com.rameses.rcp.common.*;
        
                
        public class ControlsTestController  {

            def entity = [ : ];
            
            def items = ["one", "two", "three"];
            
            void btnShowInput() {
                MsgBox.alert("entity = $entity");
            }
        
            boolean readonly;
            
            void btnToggle() {
                readonly = !readonly;
            }
                        
            void testPost() {
                MsgBox.alert("Posting $entity.testPost");
            }
            
            void save() {
                throw new Exception('session: Session Expired Error');
            }

            @Close
            def close() {
                MsgBox.alert('Closing');
            }
            
            void validateName() {
                //throw new Exception('Error.....');
            }
           
        }
        
        ]]>
    </code>
    
    <pages>
        <page template="test.client.ControlsTestPage"/>
    </pages>
</workunit>
